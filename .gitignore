# Per-machine calibrated coordinates (each PC/resolution differs).
# Commit it yourself if you want to share a calibration across identical setups.
config.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# PyInstaller build artifacts (the .exe is built per-machine via build_exe.ps1)
build/
dist/
*.spec
Firestone.exe

# OS junk
Thumbs.db
desktop.ini
.DS_Store
